<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid android:color="@color/colorTvL" />
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="0dp"
        android:color="#ccc" />
    <!-- 圆角的幅度 -->
    <corners android:radius="@dimen/px40" />

</shape>
